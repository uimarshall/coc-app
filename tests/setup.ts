/**
 * Vitest setup file — runs once before each test file.
 * We import jest-dom here so that matchers like toBeInTheDocument()
 * are available in every test automatically.
 */
import "@testing-library/jest-dom";
